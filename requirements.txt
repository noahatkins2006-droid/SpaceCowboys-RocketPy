# Space Cowboys RocketPy Simulation
# Minimal dependencies - only what's needed to run simulations

# Core simulation engine (includes built-in HRRR, KML export, FlightDataExporter)
rocketpy==1.13.0

# Data handling
numpy>=2.0.0
pandas>=2.0.0

# XML parsing for OpenRocket .ork files
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Plotting (optional, for visualization)
matplotlib>=3.8.0
